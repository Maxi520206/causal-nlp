# config/baseline_v1.yaml
exp_name: "imdb_supervised_baseline"
version: "v1.0"

data:
  path: "data/imdb.csv"
  split_ratio: [0.8, 0.1, 0.1] 
  stratify: True

reproducibility:
  seeds: [7] 

model:
  type: "tfidf_logreg"
  params:
    ngram_range: [8, 9]
    max_features: 10000
    C: 1.0
